name: 'JSON Escape'
description: 'Escape JSON strings by removing or encoding traces of special characters that could prevent parsing.'
author: 'Pieter Joost van de Sande'

branding:
  icon: 'activity'
  color: 'blue'

inputs:
  value:
    description: 'The input to escape'
    required: true

# Define your outputs here.
outputs:
  value:
    description: 'The escaped input'

runs:
  using: node20
  main: dist/index.js
